<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="20dp"
    android:paddingHorizontal="24dp"
    android:background="@drawable/bg_dialog_explanation"
    android:layout_marginHorizontal="16dp">

    <TextView
        android:id="@+id/tvBadgeExplanationDialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="DIALOG TITLE"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/tvBadgeExplanationDialogMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tvBadgeExplanationDialogTitle"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Dialog message"
        android:textSize="13sp"
        android:layout_marginTop="24dp"/>

    <TextView
        android:id="@+id/tvBadgeExplanationDialogDismiss"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/badge_explanation_ok"
        android:textSize="22sp"
        app:layout_constraintTop_toBottomOf="@+id/tvBadgeExplanationDialogMessage"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="24dp"
        android:padding="8dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>